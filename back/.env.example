# OpenAI API key for recipe extraction
OPENAI_API_KEY=sk-your-key-here

# Database configuration
# Option 1: Full DATABASE_URL (used in production/Railway)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Option 2: Individual components (for local development)
# If DATABASE_URL is not set, these will be used:
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=recipe_gantt
