services:
  - type: web
    name: nextjs-static
    runtime: static
    buildCommand: npm install; npm run build
    staticPublishPath: build
    envVars:
      - key: NODE_ENV
        value: production
